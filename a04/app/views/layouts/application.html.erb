<!DOCTYPE html>
<html>
<head>
  <title>Assessment04</title>
</head>
<body>
	
<% if flash[:errors] %>
	<p> <%= flash[:errors] %> </p>
<% end %>

<% if signed_in? %>
	<h1><%= current_user.username %></h1>
	<%= button_to("Sign Out", session_url, method: :delete) %>
<% end %>

<%= yield %>

</body>
</html>
